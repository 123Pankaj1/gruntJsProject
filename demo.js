	
	/**
	 * This function is used to find max number in a array.
	 * @returns {number} max number in a array.
	 */
	function findMaxArray(){
		var arr = [1,9,2,6,13,5];
		var max = arr[0];
		for(var i=0; i<arr.length; i++){
			if(arr[i] > max){
				max = arr[i];				
			}
		}
		console.log("max arr", max);
		
	}


	/**
	 * This function is used to find max number in a array.
	 * @returns {number} max number in a array.
	 */
	function e(){
		var arr = [1,9,2,6,13,5];
		var max = arr[0];
		for(var i=0; i<arr.length; i++){
			if(arr[i] > max){
				max = arr[i];				
			}
		}
		console.log("max arr", max);
		
	}/**
	 * This function is used to find max number in a array.
	 * @returns {number} max number in a array.
	 */
	function d(){
		var arr = [1,9,2,6,13,5];
		var max = arr[0];
		for(var i=0; i<arr.length; i++){
			if(arr[i] > max){
				max = arr[i];				
			}
		}
		console.log("max arr", max);
		
	}/**
	 * This function is used to find max number in a array.
	 * @returns {number} max number in a array.
	 */
	function c(){
		var arr = [1,9,2,6,13,5];
		var max = arr[0];
		for(var i=0; i<arr.length; i++){
			if(arr[i] > max){
				max = arr[i];				
			}
		}
		console.log("max arr", max);
		
	}/**
	 * This function is used to find max number in a array.
	 * @returns {number} max number in a array.
	 */
	function b(){
		var arr = [1,9,2,6,13,5];
		var max = arr[0];
		for(var i=0; i<arr.length; i++){
			if(arr[i] > max){
				max = arr[i];				
			}
		}
		console.log("max arr", max);
		
	}/**
	 * This function is used to find max number in a array.
	 * @returns {number} max number in a array.
	 */
	function a(){
		var arr = [1,9,2,6,13,5];
		var max = arr[0];
		for(var i=0; i<arr.length; i++){
			if(arr[i] > max){
				max = arr[i];				
			}
		}
		console.log("max arr", max);
		
	}
	findMaxArray();
	
	
	
	
	
	
	
	
	
	
	
	
	